# Historical Archive

This directory retains only the most recent historical Studio probe. Current
behavior belongs in `../current/`; the older dated audits and acceptance files
were removed after their relevant rules were incorporated into the current
contract.

Do not copy browser/CDP commands from these records into a new deployment
without re-validating them. The current production path is documented in
[`../current/`](../current/) and the current HTTP stems contract is
[`../current/STEMS_HTTP_API_AND_CLI.md`](../current/STEMS_HTTP_API_AND_CLI.md).

| Record | Status |
| --- | --- |
| [Studio 2.0 probe report](SUNO_STUDIO_2_0_PROBE_REPORT.md) | Runtime and state observations |
